// This address is public and is shipped inside the desktop game.
// Change it to your hosted TLS WebSocket address before making a release build.
window.FLAPPY_MONKEY_ONLINE = Object.freeze({
    serverUrl: 'wss://flappy-monkey-server.onrender.com'
});
